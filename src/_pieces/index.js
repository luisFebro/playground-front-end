//Null file. Change the directory location to this when finishing up a project.
import React, { useState } from 'react';

export default function Main() {
    return (
        <div>
        </div>
    );
}